[package]
name = "malecule"
version = "0.1.0"
edition = "2021"
authors = ["cleave Team"]
description = "Generate molecular visualizations from malware analysis findings"
license = "Apache-2.0"

[lints]
workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rustc-hash = "2.1"
