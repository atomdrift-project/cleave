[package]
name = "scpt"
version = "0.1.0"
edition = "2021"
authors = ["cleave Team"]
description = "Parser and symbol extractor for compiled AppleScript (.scpt) files"
license = "Apache-2.0"
repository = "https://github.com/cleave/scpt"
keywords = ["applescript", "scpt", "parser", "macos", "binary"]
categories = ["parsing", "development-tools"]

[lints]
workspace = true

[dependencies]
thiserror = "2.0"

[dev-dependencies]
