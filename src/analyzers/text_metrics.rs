//! Text metrics retired.
//!
//! All `text.*` extraction moved to expose
//! (`expose/src/formats/source/text_metrics.rs`). This module is
//! intentionally empty — kept as a no-op shim so the deletion can be
//! taken in a follow-up file removal without invalidating the cache.
