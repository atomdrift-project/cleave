# JSON Data Format
# Detects JSON structure to help exclude data files from code analysis

defaults:
  for: [all]

traits:
  - id: valid
    desc: JSON data format
    crit: baseline
    conf: 0.6
    if:
      type: raw
      regex: '^\s*\{\s*"[A-Za-z0-9_.-]{1,64}"\s*:'
      offset: 0
