traits:
  - id: js-date-setter-getter-check
    desc: Checks if Date setter works (anti-emulation)
    crit: suspicious
    conf: 0.8
    for:
      - javascript
    if:
      type: raw
      regex: 'set(UTC)?FullYear\([\s"''\d]*\).{0,50}if\s*.{0,50}get(UTC)?FullYear'