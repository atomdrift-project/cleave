defaults:
  for: [python]

traits:
  - id: exec-join-class-attributes
    desc: exec() called on joined class attributes
    crit: suspicious
    if:
      type: raw
      regex: "import\\s+marshal\\s+as\\s+[a-zA-Z0-9_]+\\s+exec\\(''\\.join\\(\\["
