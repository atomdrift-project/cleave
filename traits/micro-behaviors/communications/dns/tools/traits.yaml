# DNS Resolution Tools
# Command-line tools for DNS lookups

defaults:
  for: [all]
  crit: notable
  conf: 0.9

traits:
  - id: nslookup
    desc: nslookup command
    if:
      type: string
      word: "nslookup"

  - id: dig
    desc: dig DNS command
    if:
      type: string
      regex: '\bdig\s+'

  - id: host
    desc: host DNS command
    crit: baseline
    for: [shell, python, javascript, ruby, php, go, elf, macho]
    if:
      type: string
      regex: '\bhost\s+'
