defaults:
  for: [all]

traits:
- id: bpoorman-bearer-token
  desc: Hardcoded Bearer token for Bpoorman malware
  crit: suspicious
  conf: 1.0
  if:
    type: string
    exact: "Bearer 5bd4e8cb8165d4d2"
