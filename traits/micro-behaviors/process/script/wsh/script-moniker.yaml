defaults:
  for: [all]

traits:
  - id: script-moniker-url
    desc: "String containing a script: URL moniker"
    crit: suspicious
    if:
      type: raw
      regex: '(?i)script:https?://'

  - id: wsh-getobject-call
    desc: "String containing a GetObject call"
    crit: suspicious
    if:
      type: raw
      regex: '(?i)GetObject\s*\('
