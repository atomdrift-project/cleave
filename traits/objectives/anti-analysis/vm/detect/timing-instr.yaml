# RDTSC Instruction Detection
# Used for timing-based VM detection

defaults:
  crit: notable
  conf: 0.9

traits:
  - id: rdtsc-instruction
    desc: RDTSC instruction (read time-stamp counter)
    crit: notable
    conf: 0.95
    for: [pe, elf, macho]
    if:
      type: hex
      pattern: "0F 31"
