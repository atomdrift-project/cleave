defaults:
  for: [pe, elf, macho]

composite_rules:
  - id: go-evasive-loader
    desc: "Go loader with sandbox evasion, self-mutation, and process injection"
    crit: hostile
    all:
      - id: micro-behaviors/anti-static/obfuscate/junk-insertion::go-mutateself-func
      - id: micro-behaviors/anti-analysis/sandbox-evasion/environment::go-issandbox-func
      - id: objectives/evasion/process/injection/runtime::suspend-thread
